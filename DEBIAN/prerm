#!/bin/bash
set -e
set -x  # Debug: show commands

RULES_DIR="/usr/share/X11/xkb/rules"
DOC_DIR="/usr/share/doc/romala-keyboard"
LAYOUT_NAME="romala"

echo "Removing Romala keyboard layout entries..."

# -----------------------------
# Remove from .lst files
# -----------------------------
for FILE in base.lst evdev.lst; do
    TARGET_FILE="$RULES_DIR/$FILE"
    if grep -q "  rom             Romala" "$TARGET_FILE"; then
        sed -i "/  rom             Romala/d" "$TARGET_FILE"
        echo "→ Removed $LAYOUT_NAME from $TARGET_FILE"
    fi
done

# -----------------------------
# Remove exact XML snippet
# -----------------------------
for FILE in base.xml evdev.xml; do
    TARGET_FILE="$RULES_DIR/$FILE"
    if grep -q "<name>$LAYOUT_NAME</name>" "$TARGET_FILE"; then
        perl -0777 -i -pe 's#<layout>\s*<configItem>\s*<name>romala</name>.*?</layout>\n##s' "$TARGET_FILE"
        echo "→ Removed $LAYOUT_NAME layout block from $TARGET_FILE"
    fi
done

# -----------------------------
# Remove snippets folder
# -----------------------------
rm -rf "$DOC_DIR"

echo "🧹 Romala layout removed."

